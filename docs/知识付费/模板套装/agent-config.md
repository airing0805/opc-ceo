# Agent 配置 - {Agent 名称}

## 基本信息

| 项目 | 内容 |
|------|------|
| Agent ID | `{agent-id}` |
| 名称 | {中文名称} |
| 类型 | {skill/assistant/custom} |
| 调用方式 | {SendMessage/Team/直接调用} |

## 角色定义

### 核心职责

{核心职责描述 - 说明这个 Agent 的主要工作任务}

### 能力边界

| 能做 | 不能做 |
|------|--------|
| {能力1} | {限制1} |
| {能力2} | {限制2} |
| {能力3} | {限制3} |

### 协作关系

| 关联 Agent | 协作方式 |
|------------|----------|
| {Agent名称1} | {协作方式描述} |
| {Agent名称2} | {协作方式描述} |

## 配置参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| {参数1} | {默认值1} | {参数说明1} |
| {参数2} | {默认值2} | {参数说明2} |
| {参数3} | {默认值3} | {参数说明3} |

## 调用示例

### 基础调用
```
// 使用 SendMessage 调用
await sendMessage({
  to: "{agent-id}",
  message: "{消息内容}",
  context: {}
});
```

### 带参数调用
```
// 带参数调用示例
await sendMessage({
  to: "{agent-id}",
  message: "{消息内容}",
  context: {
    {参数1}: {参数值1},
    {参数2}: {参数值2}
  }
});
```

### Team 协作调用
```
// 使用 Team 协作
await team({
  name: "{团队名称}",
  members: ["{agent-1}", "{agent-2}"],
  task: "{任务描述}"
});
```

## 注意事项

- {注意事项1}
- {注意事项2}
- {注意事项3}
